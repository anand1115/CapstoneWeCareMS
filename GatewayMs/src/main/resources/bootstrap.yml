server:
  port: 9000

spring:
  application:
    name: GatewayMS
  cloud:
    consul:
      host: localhost
      discovery:
        hostname: localhost
        enabled: true
      config:
        enabled: true